#ifdef DESK

/**
 * @brief Emits Desk-specific WebSerial updates.
 */
void webSerialWind();
/**
 * @brief Adds Desk variables to the shared JSON document.
 */
void addProjectVariables(JsonObject variables);

#endif